import { Menu, RouterVo } from '../types/entity'

/**
 * 过滤路由
 * @param menus 菜单数据
 * @param parent_id 父ID
 */
export const filterRouter = (menus: Array<Menu>, parent_id: number) => {
  const data: Array<RouterVo> = []
  return data
}
